# ==========================================
# Gateway Service
# ==========================================

server.port=8080
spring.application.name=gateway-service
spring.profiles.active=dev


# ==========================================
# User Service Route
# Spring Cloud Gateway 2025+ binds under
# spring.cloud.gateway.server.webflux.*
# ==========================================

spring.cloud.gateway.server.webflux.routes[0].id=user-service
spring.cloud.gateway.server.webflux.routes[0].uri=http://localhost:8081

spring.cloud.gateway.server.webflux.routes[0].predicates[0]=Path=/api/user/**

# Keep /api/user when forwarding to user-service
spring.cloud.gateway.server.webflux.routes[0].filters[0]=StripPrefix=0

# Downstream services also set CORS; duplicate Access-Control-Allow-Origin
# makes browsers fail the request as a Network Error.
spring.cloud.gateway.server.webflux.default-filters[0]=DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials RETAIN_FIRST

# Frontend persona/profile calls hit /users/** on the gateway
spring.cloud.gateway.server.webflux.routes[8].id=user-service-users-alias
spring.cloud.gateway.server.webflux.routes[8].uri=http://localhost:8081
spring.cloud.gateway.server.webflux.routes[8].predicates[0]=Path=/users/**
spring.cloud.gateway.server.webflux.routes[8].filters[0]=RewritePath=/users/(?<segment>.*), /api/user/users/$\{segment}
