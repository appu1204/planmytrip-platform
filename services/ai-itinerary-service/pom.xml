<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.planmytrip</groupId>
    <artifactId>ai-itinerary-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <name>ai-itinerary-service</name>
    <description>AI Itinerary Service for PlanMyTrip</description>

    <properties>
        <java.version>17</java.version>

        <!-- Third-party dependency versions -->
        <jjwt.version>0.12.6</jjwt.version>
        <hypersistence.version>3.15.4</hypersistence.version>
        <springdoc.version>3.0.0</springdoc.version>
    </properties>

    <dependencies>

        <!-- ==================== WEB ==================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- WebClient for Gemini API -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>


        <!-- ==================== DATABASE ==================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Maps JSON columns such as plan_data.
             IMPORTANT: the artifactId is tied to your Hibernate ORM major.minor
             version, NOT your Spring Boot version. Spring Boot 4.1.0 ships
             Hibernate ORM 7.4.x (see "Hibernate ORM core version" in your
             startup log) -> use the hibernate-73 module, which covers 7.3/7.4. -->
        <dependency>
            <groupId>io.hypersistence</groupId>
            <artifactId>hypersistence-utils-hibernate-73</artifactId>
            <version>${hypersistence.version}</version>
        </dependency>


        <!-- ==================== SECURITY ==================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>


        <!-- ==================== VALIDATION ==================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>


        <!-- ==================== JWT ==================== -->

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>


        <!-- ==================== JACKSON 2 ====================
             Spring Boot 4 defaults to Jackson 3 (tools.jackson.*), but this
             service's code (GeminiClientImpl, jjwt-jackson) is written
             against classic Jackson 2 (com.fasterxml.jackson.*).

             spring-boot-jackson2 is the actual Spring Boot integration module
             that auto-configures a classic com.fasterxml.jackson.databind.ObjectMapper
             bean AND registers MappingJackson2HttpMessageConverter as an
             available option. Without this specific module, raw Jackson 2
             jars alone do nothing - Boot has no Jackson 2 auto-configuration
             to activate.

             Paired with spring.http.converters.preferred-json-mapper=jackson2
             in application.properties, this tells Spring MVC to use Jackson 2
             for all @RequestBody/@ResponseBody/ResponseEntity handling instead
             of Boot 4's default Jackson 3. -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-jackson2</artifactId>
        </dependency>


        <!-- ==================== SWAGGER / OPENAPI ==================== -->

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>


        <!-- ==================== ACTUATOR ==================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>


        <!-- ==================== LOMBOK ==================== -->

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>


        <!-- ==================== TESTING ==================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>


    <!-- ==================== BUILD ==================== -->

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>

                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>

                    </annotationProcessorPaths>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>